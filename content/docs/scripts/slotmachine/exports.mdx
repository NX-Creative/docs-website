---
title: Exports
description: Public exports registered by nx_slotmachine.
---

`nx_slotmachine` does not currently expose stable Lua exports. Player
flow runs entirely through cabinet interactions and the
[`/manageslots`](./commands#manageslots) admin command.

If you need to wire a custom entry point, the
[server events](./events) page documents the supported integration
surface.
